import 'package:flutter/material.dart';
import 'Stly.dart';

Widget buildHeader() {
  return Padding(
    padding: const EdgeInsets.only(top: 50.0),
    child: Text(
      "Home",
      style: kBaslikStyle,
    ),
  );
}
